--- crates/paper-resolver/src/lib.rs
+++ replace && with || in try_crossref
@@ -549,17 +549,17 @@
         .and_then(|v| v.as_array())
         .unwrap_or(&vec![])
     {
         let ct = link
             .get("content-type")
             .and_then(|v| v.as_str())
             .unwrap_or("");
         if ct.contains("pdf")
-            && let Some(url) = link.get("URL").and_then(|v| v.as_str())
+            || /* ~ changed by cargo-mutants ~ */ let Some(url) = link.get("URL").and_then(|v| v.as_str())
         {
             return Some(ResolvedPdf {
                 url: url.into(),
                 source: "crossref".into(),
                 downloadable: is_downloadable(url),
             });
         }
     }
